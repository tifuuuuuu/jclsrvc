# dependencies
node_modules/

# secrets (Google OAuth credentials)
.env

# runtime state (regenerated at run time; machine-specific paths)
apps.json

# git clones of deployed apps
workspaces/

# persisted per-app logs (runtime)
logs/

# editor / IDE
.vs/

# personal Claude Code settings (machine-specific)
.claude/settings.local.json
